% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Form_2RM.R
\name{form_2rm}
\alias{form_2rm}
\title{Develop a two-regression algorithm}
\usage{
form_2rm(data, activity_var, sed_cp_activities, sed_activities, sed_cp_var,
  sed_METs, cwr_activities, cwr_cp_var, met_var, cwr_formula, ila_formula)
}
\arguments{
\item{data}{The data with which to develop the algorithm}

\item{activity_var}{Character scalar. Name of the variable defining which
activity is being performed}

\item{sed_cp_activities}{Character vector. Activities to be included in the
process of forming the sedentary classifier}

\item{sed_activities}{Character vector. Actual sedentary activities}

\item{sed_cp_var}{Character scalar. Name of the variable on which the
sedentary cut-point is defined}

\item{sed_METs}{Numeric scalar. Metabolic equivalent value to apply to
sedentary activities}

\item{cwr_activities}{Character vector. Actual ambulatory activities}

\item{cwr_cp_var}{Character scalar. Name of the variable on which the
walk/run cut-point is defined}

\item{met_var}{Character scalar. Name of the variable giving actual energy
expenditure (in metabolic equivalents)}

\item{cwr_formula}{Character scalar. Formula to use for developing the
walk/run regression model}

\item{ila_formula}{Character scalar. Formula to use for developing the
intermittent activity regression model}
}
\value{
An object of class `TwoRegression`
}
\description{
Develop a two-regression algorithm
}
\examples{
\dontrun{
data(ag_metabolic_s1, package = "FLPAYr")
test_data <- subset(ag_metabolic_s1, site == "hip")
form_2rm(
data = test_data,
activity_var = "Behavior",
sed_cp_activities = c("Internet", "Reclining",
"Sweep", "Book", "Games", "Lying", "Dust"),
sed_activities = c("Internet", "Reclining", "Book", "Games", "Lying"),
sed_cp_var = "ENMO",
sed_METs = 1.25,
cwr_activities = c("Run", "Walk_Slow", "Walk_Brisk"),
cwr_cp_var = "ENMO_CV10s",
met_var = "MET_RMR",
cwr_formula = "MET_RMR ~ ENMO",
ila_formula = "MET_RMR ~ I(ENMO)+I(ENMO^2)+I(ENMO^3)"
)
}
}
\seealso{
Links to methods for `TwoRegression` should go here (predict, plot,
  summary)
}
